//definition of interface for the home page and other pages.

export interface homeTypes {
  flexPosition: string;
  headerBg: string;
  imgStyles: string;
  rawFlex: string;
  whiteText: string;
  textSize: string;
  headingSize: string;
  wrapText: string;
  defaultBg: string;
}
